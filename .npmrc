# @lunarphp/panel-vite-plugin@0.1.1 khai peer vite ^5||^6 trong khi dự án dùng
# vite 7. Plugin chỉ dùng hook `config` + `writeBundle` (API ổn định từ Vite 2)
# nên chạy tốt trên 7; dải peer chỉ là khai báo cũ ở thượng nguồn. Ghim ở đây để
# `npm ci` trên CI/máy khác không gãy vì ERESOLVE.
legacy-peer-deps=true
